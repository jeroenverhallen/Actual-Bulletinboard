extends ./index
block content
    h1 Messageboard... More like message bored, amirite? I'm sorry, that was terrible
    hr
    .container
        each post in posts
            .card.card-block
                a( href="/" + post.title )
                    h( class="card-title" )= post.title
                p( class="card-text" )= post.message